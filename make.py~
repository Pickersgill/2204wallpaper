import numpy as np
import random
from PIL import Image, ImageDraw

N = 50
COLS = 10
ROWS = 10
UNIT = 30
BUFF = 2

COLOURS = ["red", "green", "blue"]
grid = [[0 for x in range(COLS)] for y in range(ROWS)]

img = Image.new("RGBA", (UNIT * COLS, UNIT * ROWS), "white")
drw = ImageDraw.Draw(img)

for i in range(N):
    x = random.randint(0, COLS-1)
    y = random.randint(0, ROWS-1)
    grid[x][y] = random.choice(COLOURS)
    
print(grid)
#drw.ellipse([x, y, x + UNIT - (2 * BUFF), y + UNIT - (2 * BUFF)], fill="red")    
#img.show()


